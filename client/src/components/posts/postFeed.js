import React from "react";

const PostFeed = ({ posts }) => {
  //   return posts.map((post) => <PostItem key={post._id} posts={posts} />);
  return (
    <div>
      <h1>hello</h1>
    </div>
  );
};

export default PostFeed;
